server {
    listen 80;
    server_name ${SERVER_NAME};

    root /var/www/html;
    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~* \.(jpg|jpeg|gif|css|png|js|ico|html)$ {
        expires max;
        log_not_found off;
    }

    # Gestion spécifique pour l'admin
    location ~ ^/admin[0-9a-z]+/ {
        try_files $uri $uri/ /admin9400esplqtdqoopx53a/index.php?$args;
    }
}

# Configuration pour HTTPS (si SSL est configuré)
# server {
#     listen 443 ssl;
#     server_name localhost;

#     ssl_certificate /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;

#     root /var/www/html;
#     index index.php index.html index.htm;

#     access_log /var/log/nginx/prestashop_access.log;
#     error_log /var/log/nginx/prestashop_error.log;

#     client_max_body_size 64M;

#     location / {
#         try_files $uri $uri/ /index.php?$args;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php:9000;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }

#     location ~ /\.ht {
#         deny all;
#     }
# }